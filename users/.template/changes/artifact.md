# artifact.md — Change Log

> Tracks how the user's artifact has evolved across sessions. Newest entries on top, oldest at the bottom.
>
> Current artifact lives in `../artifact.md`. We do not preserve per-session artifact snapshots — the artifact itself evolves in place. This change log preserves the narrative of how the picture has shifted over time, so the user can still see their evolution without us storing N versions of the artifact.

## YYYY-MM-DD (Session N) — Initial creation
- Artifact generated during Session 1's post-session capture step.
- (Initial fields populated from session content.)
