# Inactive Profile Change Logs

> Change logs for profile pages that have been retired. When a profile page is moved from `../../profiles/` to `../../inactive-profiles/`, its change log is moved from `../profiles/` to here.
>
> The final entry in each log records the deactivation event — when and why it was retired.
